<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="300dp"
    android:layout_height="315dp"
    android:background="@drawable/ledi_login_agreement_bg" >

    <LinearLayout
        android:layout_width="300dp"
        android:layout_height="240dp"
        android:orientation="vertical" 
        android:gravity="center">
        <LinearLayout android:layout_width="match_parent"
            android:layout_height="40dp"
            android:orientation="vertical"
            android:gravity="center"
            
            android:background="@drawable/ledi_login_agreement_title">
		<TextView
		    android:layout_width="match_parent"
		    android:layout_height="wrap_content"
		    android:gravity="center"
		    android:text="44755用户协议"
		    android:textSize="15sp"
		    android:textColor="#000000"/>
		</LinearLayout>
        <WebView
            android:id="@+id/agreement_webview"
            android:layout_marginTop="8dp"
            android:layout_width="fill_parent"
            android:layout_height="fill_parent" />
    </LinearLayout>

    <Button
        android:id="@+id/agreement_sure"
        android:layout_width="272dp"
        android:layout_height="41dp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="15dp"
        android:layout_marginBottom="15dp"
        android:background="@drawable/ledi_login_retrieve_tongyi"
        android:text="同意"
        android:textColor="#ffffff"
        android:textSize="16sp" />

</RelativeLayout>